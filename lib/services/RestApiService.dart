import 'dart:io';

import '../helper/RestApiRequestWrapper.dart';
import '../helper/RestApiResponseWrapper.dart';

abstract class RestApiService {
  // RestApiResponseWrapper makeCallout(RestApiRequestWrapper requestWrapper);

  // HttpResponse apiCall(String endpoint, String method, String body, String bearerToken);

}